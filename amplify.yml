version: 1
backend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands: []
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  start:
    command: npm start