{
  "name": "csp-express-html-test",
  "version": "1.0.0",
  "description": "CSP violation testing tool",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon index.js",
    "start": "node index.js",
    "build": "mkdir -p dist && cp -r public/* dist/ && cp index.html dist/ || echo 'Build completed'"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "nodemon": "^3.1.9"
  }
}